# Example environment file for print agent
# Copy this to .env and edit values as needed

# Port the agent will listen on
PRINT_AGENT_PORT=5000

# CORS origins (comma separated) or '*' to allow all origins for testing
# Example: PRINT_AGENT_CORS_ORIGINS=http://localhost:8009
PRINT_AGENT_CORS_ORIGINS=http://localhost:8009

# Optional: path to SumatraPDF.exe (if you want Sumatra fallback)
# SUMATRA_PATH=C:\\Program Files\\SumatraPDF\\SumatraPDF.exe
